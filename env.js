// env.js — генерируется Vercel при сборке
// Не редактируй этот файл вручную

window.ENV = {
    SUPABASE_URL:      '{{SUPABASE_URL}}',
    SUPABASE_ANON_KEY: '{{SUPABASE_ANON_KEY}}',
    HOST_PASSWORD:     '{{HOST_PASSWORD}}'
};